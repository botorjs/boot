import { ServiceProvider } from "./ServiceProvider";
import { Boot } from "./boot";

export class Loader {
    private _poor_provinder:  Array<ServiceProvider>;
    private _poor_obj_provinder: ServiceProvider[];
    private _app: Boot;

    public add(provider: ServiceProvider| ServiceProvider[]) {

    }

    _register() {

    }

    _boot() {

    }

    preload() {
        
    }

}